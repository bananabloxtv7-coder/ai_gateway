# Optional: the single key your apps present to the gateway.
# Leave empty for local, no-auth use.
GATEWAY_MASTER_KEY=my-secret-gateway-key

# Optional: path to the providers/keys config (default: ./keys.json)
GATEWAY_CONFIG=./keys.json
